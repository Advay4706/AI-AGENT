# Copy to .env and fill in when running the LLM agent (Step 3+) live.
# The .env file is gitignored. The code also reads ANTHROPIC_API_KEY straight
# from the environment, so exporting it in your shell works too.
ANTHROPIC_API_KEY=
