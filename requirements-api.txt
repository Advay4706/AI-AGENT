# Runtime dependencies for the FastAPI screening service (Docker image).
# The full dev/eval set (Faker, pandas, scikit-learn, gradio, pytest) lives in
# requirements.txt and is not needed to serve /screen.
fastapi>=0.110
uvicorn[standard]>=0.29
anthropic>=0.40
pydantic>=2.7
rapidfuzz>=3.9
jellyfish>=1.0
unidecode>=1.3
